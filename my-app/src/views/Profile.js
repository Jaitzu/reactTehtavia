import React from 'react';
import Nav from '../components/Nav.js';

const Profile = (props) => {
    return (
        <React.Fragment>
            <Nav/>
            <h1>Profile</h1>
        </React.Fragment>
    );
};

export default Profile;